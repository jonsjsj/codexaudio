.gradle/
build/
local.properties
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.keystore
*.jks

# Committed DEBUG-ONLY keystore so every CI build is update-compatible (see app/build.gradle.kts)
!app/audex-debug.keystore
